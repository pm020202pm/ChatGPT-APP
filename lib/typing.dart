import 'package:flutter/material.dart';
class Typing extends StatelessWidget {
  const Typing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
          child: Text("typing...", style: TextStyle(fontSize:13, color: Colors.white),),
        );
  }
}
